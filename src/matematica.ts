function somar(x: number,y: number): number{
    return x + y;
}

//fazer mais duas funções de subtrair e multiplicar

function subtrair(x: number,y: number): number{
    return x - y
}

function multiplicar(x: number,y: number): number{
    return x * y
}

export default{
    somar,
    subtrair,
    multiplicar
}